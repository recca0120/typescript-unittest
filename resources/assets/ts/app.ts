import {Facebook} from './facebook';
import {Auth} from './auth';
import {HttpClient} from './httpclient';

const win: any = window;

win.Facebook = Facebook;
win.Auth = Auth;
win.HttpClient = HttpClient;
